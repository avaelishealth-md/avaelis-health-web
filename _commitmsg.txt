pre-launch QA fixes: go-live switches, talk-summary, security, SEO, a11y

Autonomous fixes from the 16 Jun pre-launch audit (blocker + high + safe).

Go-live switches (now env-driven, default safe):
- layout.tsx: noindex via NEXT_PUBLIC_INDEXABLE (default noindex); title template.
- middleware.ts: COMING_SOON via env; admin gate now enforces the allow-list
  (was: any logged-in user could load /admin and read drafts).

Talk-day QR flow:
- /api/talk-summary: fail loud if lead capture fails (was: always ok:true);
  return whether the email actually sent.
- TalkSummaryForm: success copy driven by real send result (no false
  "check your inbox" when Resend is unconfigured).

Security:
- 0003_lock_rls_to_admin.sql: scope posts + post-images RLS to an admins
  table (0001 granted full CRUD to ANY authenticated user). RUN ON DANNY'S DB.
- login: shouldCreateUser:false so unknown emails can't bootstrap an account.
- /api/ingest: constant-time secret comparison.

SEO/metadata: www->apex 301 + AVIF/WebP formats (next.config); per-page
canonical + OpenGraph (about/writing/talk-summary/post); Article publisher +
mainEntityOfPage; MedicalClinic JSON-LD on the home page.

Content: Evidence Index now qualitative (Strong/Moderate/Early) instead of
fabricated 92/58/46 scores.

A11y: skip-to-content link + <main> landmark on marketing pages; keyboard
:focus-visible ring; journey headings h2->h3 (no level skip); redundant
cover-image alt removed; sanitiser no longer allows <h1> in post bodies.

Polish: em dashes removed from subscribe error strings; Mailchimp tag/note
failures now logged; copy consistency (Dr./preventive); custom 404 + error
boundary; web manifest.
